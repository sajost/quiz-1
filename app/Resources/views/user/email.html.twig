{# src/AppBundle/Resources/views/default/username.html.twig #} 
{% extends 'default/init.html.twig' %} 


{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block js %}
    {{ parent() }}
{% endblock %}

{# put input + label of checkbox in the div#}
{% if form is defined %}
    {% form_theme form  'admin/form.fields.html.twig' %}
{% endif %}

{% block body %}
	<div id="" class="container">

		<div id="panel" class="panel panel-default">
			<div class="panel-heading">
            	<h3 class="panel-title">Mein Profil</h3>
            </div>
			<div class="panel-body">
        	
               <form action="{{ path('my_email') }}" method="post" class="" id="f-my-email" data-toggle="validator" role="form">
                	
                	<div class="form-group">
        				<label for="{{ form.email.vars.id }}">Email</label>
        				{{ form_widget(form.email) }}
        				<div class="help-block with-errors">{{ form_errors(form.email) }}</div>
        			</div>
                	
                	
                	<div style="display:none ">
                	    {{ form_rest(form) }}
                	</div>
            		<div class="form-group">
            			<a class="btn btn-default btn-sm" href="{{ path('my_user') }}">Zurück</a>
            			<button class="btn btn-primary btn-sm" type="submit">Speichern</button>
            		</div>
                
            		<script type="text/javascript">
            		//
            		$( "#" ).each(function(index) {
            			
            		 });
            		</script>
               </form>
			</div>
		</div>
		
	</div>


{% endblock %} 