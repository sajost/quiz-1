{# src/Resources/views/admin/q.tag.html.twig #} 
{% extends 'admin/init.html.twig' %} 


{% block body %}
	<div id="" class="container">

		<div id="panel" class="panel panel-default">
			<div class="panel-heading">
            	<h3 class="panel-title">Da ist die ganze Liste von allen Frage, kLicken auf Titel zum Bearbeiten, , kLicken auf ID zum Fragenornden</h3>
            </div>
			<div class="panel-body">
				<div class="panel-scroll">
				<table class="table table-bordered" fixed-header>
					<thead>
                        <tr>
                          <th style="width:50px">#</th>
                          <th style="width:250px">Titel</th>
                        </tr>
                    </thead>
                    {% for e in quizs %}
                    	<tr>
                    		<td style="width:50px">
                    			<a class="" href="{{ path('admin_quizquestion_all',{'eid':e.id}) }}" style="">{{ e.id }}</a>
                    		</td>
                    		<td style="width:250px">
                    			<a class="" href="{{ path('admin_quize',{'eid':e.id}) }}" style="">{{ e.title }}</a>
                    		</td>
                		</tr>
                	{% endfor %}
				</table>
            	</div>
            	<br/>
        	
                <form action="{{ path('admin_quiz') }}" method="post" id="f-admin-quiz" data-toggle="validator" role="form">
                	
            		<h3>Da kannst eine neue Quiz erstellen und speichern</h3>
            		<br/>
            		
            		<div class="form-group">
        				<label for="{{ form.title.vars.id }}">Fragetext</label>
        				{{ form_widget(form.title) }}
        				<div class="help-block with-errors">{{ form_errors(form.title) }}</div>
        			</div>
                	
                	<div class="form-group">
        				<label for="{{ form.loginrequired.vars.id }}">Login notwendig</label>
        				{{ form_widget(form.loginrequired) }}
        				<div class="help-block with-errors">{{ form_errors(form.loginrequired) }}</div>
        			</div>
        			
        			<div class="form-group">
        				<label for="{{ form.roundperday.vars.id }}">wie viele Runden pro Tag gespielt werden k�nnen</label>
        				{{ form_widget(form.roundperday) }}
        				<div class="help-block with-errors">{{ form_errors(form.roundperday) }}</div>
        			</div>
                    
            		<div class="form-group">
        				<label for="{{ form.hsnumber.vars.id }}">Platzierungen</label>
        				{{ form_widget(form.hsnumber) }}
        				<div class="help-block with-errors">{{ form_errors(form.hsnumber) }}</div>
        			</div>
        			
        			<div class="form-group">
        				<label for="{{ form.analysis.vars.id }}">Auswertung des Ergebnisses</label>
        				{{ form_widget(form.analysis) }}
        				<div class="help-block with-errors">{{ form_errors(form.analysis) }}</div>
        			</div>
        			
        			<div class="form-group">
        				<label for="{{ form.reward.vars.id }}">Belohnung</label>
        				{{ form_widget(form.reward) }}
        				<div class="help-block with-errors">{{ form_errors(form.reward) }}</div>
        			</div>
        			
        			<div class="form-group">
        				<label for="{{ form.sharehs.vars.id }}">Highscore teilen</label>
        				{{ form_widget(form.sharehs) }}
        				<div class="help-block with-errors">{{ form_errors(form.sharehs) }}</div>
        			</div>
        			
        			<div class="form-group">
        				<label for="{{ form.shareanalysis.vars.id }}">Auswertung teilen</label>
        				{{ form_widget(form.shareanalysis) }}
        				<div class="help-block with-errors">{{ form_errors(form.shareanalysis) }}</div>
        			</div>
        			
        			<div class="form-group">
        				<label for="{{ form.sharereward.vars.id }}">Belohnung teilen</label>
        				{{ form_widget(form.sharereward) }}
        				<div class="help-block with-errors">{{ form_errors(form.sharereward) }}</div>
        			</div>
            		
            		<div class="form-group">
        				<label for="{{ form.joker5050.vars.id }}">Joker "50:50"</label>
        				{{ form_widget(form.joker5050) }}
        				<div class="help-block with-errors">{{ form_errors(form.joker5050) }}</div>
        			</div>
        			
        			<div class="form-group">
        				<label for="{{ form.jokerpause.vars.id }}">Joker "Zeit stoppen"</label>
        				{{ form_widget(form.jokerpause) }}
        				<div class="help-block with-errors">{{ form_errors(form.jokerpause) }}</div>
        			</div>
            		
            		<div class="form-group">
        				<label for="{{ form.jokerskip.vars.id }}">Joker "Frage überspringe"</label>
        				{{ form_widget(form.jokerskip) }}
        				<div class="help-block with-errors">{{ form_errors(form.jokerskip) }}</div>
        			</div>
            		
            		<div class="form-group">
        				<label for="{{ form.difficulty.vars.id }}">Schwierigkeitsstufe</label>
        				{{ form_widget(form.difficulty) }}
        				<div class="help-block with-errors">{{ form_errors(form.difficulty) }}</div>
        			</div>
        			
        			<div class="form-group">
        				<label for="{{ form.timelimit.vars.id }}">Zeitlimit in Sekunden</label>
        				{{ form_widget(form.timelimit) }}
        				<div class="help-block with-errors">{{ form_errors(form.timelimit) }}</div>
        			</div>
        			
        			<div class="form-group">
        				<label for="{{ form.random.vars.id }}">Reihenfolge zufällig</label>
        				{{ form_widget(form.random) }}
        				<div class="help-block with-errors">{{ form_errors(form.random) }}</div>
        			</div>
        			
        			<div class="form-group">
        				<label for="{{ form.repeat.vars.id }}">Fragen wiederholen</label>
        				{{ form_widget(form.repeat) }}
        				<div class="help-block with-errors">{{ form_errors(form.repeat) }}</div>
        			</div>
        			
        			<div class="form-group">
        				<label for="{{ form.trueanswer.vars.id }}">Frage-Typs</label>
        				{{ form_widget(form.trueanswer) }}
        				<div class="help-block with-errors">{{ form_errors(form.trueanswer) }}</div>
        			</div>
            		
            		{{ form_row(form.cats) }}
            		
                	
                	<div style="display: none">
                	    {{ form_rest(form) }}
                	</div>
            		<div class="f-admin">
            			<button class="btn btn-primary btn-lg" type="submit">Speichern</button>
            		</div>
                
            		<script type="text/javascript">
            		//
            		$( "#" ).each(function(index) {
            			
            		});
            		</script>
            	</form>
			</div>
		</div>
		
	</div>
{% endblock %} 