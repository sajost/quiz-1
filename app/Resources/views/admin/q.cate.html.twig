{# src/AppBundle/Resources/views/Admin/carlogcate.html.twig #} 
{% extends 'admin/init.html.twig' %} 

{% block body %}
	<div id="" class="container">

		<div id="panel" class="panel panel-default">
			<div class="panel-heading">
            		<a class="btn btn-link" href="{{ path('admin_qcat') }}" style="">Back to Categories</a>
            </div>
			<div class="panel-body">
        	
               {{ form_start(form) }}
                	{{ form_errors(form) }}
                	
                	<h3>Here u can edit Question-Category and save</h3>
                	{{ form_row(form.title) }}
                	<br/><br/>
                	
                	
                	<div style="display: none">{{ form_rest(form) }}</div>
            		<div class="form-field">
            			<button class="btn btn-primary btn-lg" type="submit">Speichern</button>
            		</div>
                
            		<script type="text/javascript">
            		//
            		$( "#" ).each(function(index) {
            			
            		 });
            		</script>
               {{ form_end(form) }}
			</div>
		</div>
	</div>
   
{% endblock %} 