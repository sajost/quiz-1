{% extends 'default/init.html.twig' %} 


{% block body %}
	<div id="" class="container"> 

		<div id="panel" class="panel panel-default">
			<div class="panel-body">
				<div class="text-center">
					<h3 class="">{{ quiz_last.title }} Start-Seite</h3>
        			<a class=" btn btn-link" href="{{ path('start0') }}" id="btn-login"> >> Jetzt spielen</a>
        		</div>
        	</div>
		</div>
		
	</div>
{% endblock %} 